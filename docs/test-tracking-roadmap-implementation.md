# 测试跟踪表 — feat/roadmap-implementation

> 分支：`feat/roadmap-implementation`　基线：`main`　范围：本迭代 ROADMAP 14 项（C1–C6、N1–N8）。
> 应用类型：Tauri 桌面客户端（Tauri IPC + Vue 页面，无 HTTP 接口、无数据库断言）。用例仅覆盖**页面可见行为 / 桌面级可观测行为**。
> 前置统一说明：除特别注明外，均为「已安装 sing-box 内核 + 至少一个可用订阅 + 代理可正常连通」。

| 编号 | 模块 | 功能点 | 功能描述 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 测试结果 | 备注 |
|------|------|--------|----------|----------|----------|----------|--------|----------|------|
| WEB-001 | 设置 - 语言(C1) | 切换语言即时生效 | 验证中/英切换实时刷新界面 | 应用已启动，停留在设置页 | 1. 设置→语言 选择 English<br>2. 观察侧栏与各页标题<br>3. 切回 简体中文 | 1. 选中后**无需重启**，侧栏(Dashboard/Subscriptions/…)、设置项标题立即变英文<br>3. 切回后立即恢复中文 | P1 | 待测 | i18n 全量 |
| WEB-002 | 设置 - 语言(C1) | 语言持久化 | 验证语言选择重启后保留 | 已将语言设为 English | 1. 关闭并重新打开应用 | 启动后界面仍为 English（localStorage + AppConfig 双持久化） | P1 | 待测 | |
| WEB-003 | 设置 - 语言(C1) | 各页面文案无残留中文(英文模式) | 抽查所有视图已国际化 | 语言=English | 1. 逐页打开 仪表盘/订阅/节点/连接/流量统计/日志/分流规则/设置 | 各页可见文案均为英文，无硬编码中文残留 | P2 | 待测 | 8 视图 + 侧栏 |
| WEB-010 | 流量统计(N1) | 统计页入口与展示 | 验证新增「流量统计」页 | 侧栏可见 | 1. 点击侧栏「流量统计」 | 进入统计页，显示 今日/下载/上传/合计 四张汇总卡 + 每日流量堆叠柱状图 | P1 | 待测 | 新增 Stats.vue + 路由 |
| WEB-011 | 流量统计(N1) | 流量累积与持久化 | 验证用量按日累积且重启不丢 | 代理运行并产生流量 | 1. 开代理浏览若干流量<br>2. 等待≥30s<br>3. 进统计页看「今日」<br>4. 重启应用再看 | 2-3. 今日上下行随使用增长（约每 30s 落盘一次）<br>4. 重启后历史仍在（traffic_stats.json） | P1 | 待测 | |
| WEB-012 | 流量统计(N1) | 时间范围切换 | 验证 7/30/90 天切换 | 已有多日数据(可选) | 1. 点击 7天 / 30天 / 90天 | 柱状图与「近 N 天」汇总随之变化 | P2 | 待测 | |
| WEB-020 | 订阅 - 过滤(N2) | 新增订阅时排除节点 | 验证 exclude 正则剔除无用节点 | 有一个含「官网/剩余流量」占位节点的订阅链接 | 1. 添加订阅→展开「节点过滤/分组」<br>2. 排除填 `流量\|官网\|过期`<br>3. 添加 | 占位节点被剔除，仅保留真实节点；节点数与列表相应减少 | P1 | 待测 | |
| WEB-021 | 订阅 - 过滤(N2) | 仅保留(include)正则 | 验证 include 只留匹配节点 | 同上订阅 | 1. 仅保留填 `香港\|新加坡`<br>2. 添加 | 只保留名称含香港/新加坡的节点 | P1 | 待测 | |
| WEB-022 | 订阅 - 过滤(N2) | 非法正则不清空 | 验证错误正则不致丢光节点 | 任一订阅 | 1. 仅保留填 `[invalid`（非法）<br>2. 添加 | 视为无过滤，节点全部保留（不被清空） | P1 | 待测 | 安全兜底 |
| WEB-023 | 订阅 - 过滤(N2) | 按地区分组 | 验证 group_by_region 生效 | 含多地区节点的订阅 | 1. 勾选「按地区自动分组」<br>2. 添加<br>3. 看节点分组 | 节点 group 变为 香港/日本/美国/其他 等地区标签 | P2 | 待测 | |
| WEB-024 | 订阅 - 过滤(N2) | 已存在订阅离线重过滤 | 验证过滤按钮对缓存重应用 | 已添加过的订阅 | 1. 订阅项点「过滤」按钮(漏斗)<br>2. 改 include/exclude<br>3. 保存并应用 | 无需重新拉取，立即按新规则重算节点；过滤按钮高亮表示已设置 | P1 | 待测 | cmd_set_subscription_filters |
| WEB-030 | 节点 - WireGuard(N3) | 导入 WG 节点 | 验证 wireguard 链接/Clash 解析 | 有一个 wireguard:// 链接或含 wireguard 的 Clash 订阅 | 1. 手动导入该链接/订阅<br>2. 看节点列表 | 出现协议为 wireguard 的节点（名称/服务器正确） | P1 | 待测 | 需真实内核确认 endpoint schema |
| WEB-031 | 节点 - WireGuard(N3) | WG 节点连通 | 验证选中 WG 节点可代理 | 已导入 WG 节点且配置有效 | 1. 选中 WG 节点<br>2. 开启系统代理<br>3. 访问外网/设置页诊断 | 代理连通，出口为 WG 落地（诊断出口 IP 符合预期） | P0 | 待测 | 依赖内核接受 endpoints 配置 |
| APP-040 | 设置 - 全局快捷键(N4) | 开关与默认绑定 | 验证启用后快捷键切换系统代理 | 应用运行，无冲突占用组合键 | 1. 设置→系统行为→开启「全局快捷键」<br>2. 窗口最小化<br>3. 按 Ctrl/Cmd+Shift+P | 系统代理在「开↔关」间切换（托盘/仪表盘状态随之变化） | P1 | 待测 | macOS 用 Cmd |
| APP-041 | 设置 - 全局快捷键(N4) | TUN 与模式快捷键 | 验证 +T 切 TUN、+R 切模式 | 已启用全局快捷键；TUN 具备权限 | 1. 按 Ctrl/Cmd+Shift+T<br>2. 按 Ctrl/Cmd+Shift+R 多次 | 1. TUN 模式开↔关切换<br>2. 代理模式在 规则→全局→直连 循环 | P2 | 待测 | |
| APP-042 | 设置 - 全局快捷键(N4) | 关闭后释放 | 验证关闭开关后快捷键失效 | 已启用过快捷键 | 1. 关闭「全局快捷键」开关<br>2. 再按 Ctrl/Cmd+Shift+P | 快捷键不再触发任何切换（unregisterAll） | P2 | 待测 | |
| WEB-050 | 设置 - 网络诊断(N5) | 运行诊断(代理开) | 验证出口信息与连通性探测 | 代理已运行 | 1. 设置→网络诊断→运行诊断<br>2. 等待结果 | 显示 出口 IP / 地区 / ISP，及 Google/YouTube/GitHub/Cloudflare 探测色点(绿=可达含延迟，红=不可达) | P1 | 待测 | 依赖外部 ip-api/探测端点 |
| WEB-051 | 设置 - 网络诊断(N5) | 未开代理时报错 | 验证前置校验 | 代理未运行 | 1. 直接点「运行诊断」 | 提示「代理未运行，无法诊断」，不崩溃 | P2 | 待测 | |
| WEB-060 | 设置 - 配置Profile(N6) | 保存当前为 Profile | 验证快照保存 | 当前已有订阅/规则等设置 | 1. 设置→配置Profile→输入名称(如 家庭)<br>2. 保存当前 | 列表出现「家庭」条目；profiles/家庭.json 落盘 | P1 | 待测 | |
| WEB-061 | 设置 - 配置Profile(N6) | 切换 Profile | 验证一键切换并刷新状态 | 已存在≥2个 Profile | 1. 改动当前设置并存为另一 Profile<br>2. 点某 Profile「切换」 | 订阅/节点/规则/设置切换为该 Profile，界面刷新；提示「下次启动代理生效」 | P1 | 待测 | 路由/DNS 重启代理后生效 |
| WEB-062 | 设置 - 配置Profile(N6) | 删除 Profile | 验证删除与名称安全 | 已存在 Profile | 1. 点某 Profile 删除→确认 | 该条从列表消失；非法名(含 / \ ..)无法创建对应文件 | P2 | 待测 | sanitize_profile_name |
| WEB-070 | 节点 - 健康度(N7) | 延迟色点 | 验证节点名前健康色点 | 已添加节点 | 1. 节点页对节点测延迟<br>2. 观察节点名前圆点 | 圆点按延迟着色：绿(<100ms)/橙(<300ms)/红(更高)/灰(未测)；悬停显示延迟值 | P2 | 待测 | |
| APP-080 | 应用 - 单实例(N8) | 二次启动唤起 | 验证不开第二个进程 | 应用已在运行 | 1. 再次双击启动应用 | 不新建窗口/进程，已有主窗口被显示并聚焦(取消最小化) | P1 | 待测 | tauri-plugin-single-instance |
| WEB-090 | 连接 - 增强(C5) | 列排序 | 验证表头点击排序 | 代理运行且有活动连接 | 1. 连接页点「上传/下载/主机/协议」表头<br>2. 再点一次 | 按该列升/降序排序，表头显示 ▲/▼ 箭头 | P2 | 待测 | |
| WEB-091 | 连接 - 增强(C5) | 流量总计 | 验证头部上下行合计 | 有活动连接 | 1. 观察连接页头部「总计 ↑x ↓y」 | 实时显示当前(过滤后)连接的上下行总和 | P2 | 待测 | |
| WEB-092 | 连接 - 增强(C5) | 按主机聚合 | 验证聚合视图与一键关闭 | 同一站点多连接 | 1. 切「按主机聚合」<br>2. 看某 host 的连接数/流量<br>3. 点该行关闭 | 2. 按 host 汇总连接数与流量<br>3. 关闭该 host 的全部连接 | P2 | 待测 | |
| WEB-100 | 分流规则 - 拖拽(C6) | 拖拽排序 | 验证拖动改优先级 | 规则≥2条 | 1. 规则页拖动某规则到新位置(用手柄)<br>2. 点「保存并应用」 | 拖动时有视觉反馈，序号(优先级)随之更新；保存后顺序生效(代理运行需重启) | P1 | 待测 | 顺序=匹配优先级 |
| WEB-110 | 设置 - 内核更新(C4) | 内核下载校验 | 验证 SHA-256 完整性校验 | 可访问 GitHub | 1. 设置→sing-box 内核管理→检查/下载更新 | 正常下载并安装；若文件被篡改/损坏则报「内核校验失败…已丢弃」并不替换 | P1 | 待测 | 正常路径必测；篡改为构造场景 |
| WEB-111 | 设置 - 应用更新(C3) | 安装包下载校验 | 验证安装包 SHA-256 校验 | 存在新 release | 1. 设置→应用更新→下载并安装 | 正常下载后启动安装器；校验不一致则提示「安装包校验失败…已取消安装」，不启动安装器 | P1 | 待测 | 需真实新版本 release |
| WEB-120 | 系统代理 - Linux(C2) | GNOME 系统代理 | 验证 Linux 下系统代理可设 | Linux + GNOME 桌面 (.deb) | 1. 开启系统代理<br>2. 看 GNOME 设置→网络→代理 | 代理模式=手动，http/https/socks 指向 127.0.0.1:端口；关闭后恢复 none | P1 | 待测 | **仅 Linux/GNOME**；KDE 暂不支持 |
| REG-200 | 回归 - 核心代理 | 基础代理不回归 | 验证既有功能未被破坏 | 正常环境 | 1. 开/关 系统代理、TUN<br>2. 切 规则/全局/直连<br>3. 测延迟/测速、连接/日志页 | 与改动前行为一致，无报错、无崩溃 | P0 | 待测 | 重点回归 |
